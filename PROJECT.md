# Yauza Photofactory — источник истины

Документ фиксирует архитектуру и порядок работ. **Любое решение сначала здесь (или в `docs/`), потом в коде.**

## Зачем этот репозиторий

Пошаговая «Фотофабрика» для Yauza Media: FTP → пачки → уведомления → веб для бильдов → позже сессии, фотобанк, PWA.

Стек: **Python 3.11+**, **FastAPI** (HTTP API и позже веб), фоновый **batch-watcher**, **PostgreSQL**, **vsftpd** на VPS.

## Принципы (кратко)

1. Один пароль FTP на всю зону `incoming/`; фотограф = **имя подпапки** (задаётся в камере, папка создаётся при первой заливке).
2. Все настраиваемые величины — в `config.yaml` (образец: `config.example.yaml`).
3. Модули сменяемые: уведомления, экспорт на Яндекс.Диск, хранилище.
4. Совместимость со старым процессом: скачать ZIP, работать локально.
5. Документ → код → проверка на VPS.

## Термины (MVP)

| Термин | В MVP |
|--------|--------|
| **Photographer** | Имя подпапки под `incoming/` (без отдельного аккаунта в БД на старте — можно завести запись при первом файле). |
| **Batch (пачка)** | Файлы одного фотографа, между которыми не было новых файлов **60 с** (по умолчанию, настраивается). |
| **Photo** | Один JPEG в пачке. |
| **Incoming** | `incoming/<photographer>/` — сюда льёт камера; файлы остаются до ручного удаления бильдом или удаления после скачивания через кабинет. |
| **Originals** | `originals/<event_id>/<session_id>/<batch_id>/` — после этапа с сессиями; на MVP-0 можно `originals/default/unnamed/<batch_id>/`. |
| **Backup** | Локальная серверная копия (опционально; для текущего продуктового режима по умолчанию выключаем и доверяем зеркалу Я.Диска). |

## Структура каталогов на сервере (целевая)

```
/srv/yauza/
  incoming/              # FTP chroot, upload-only
    <photographer>/      # создаётся камерой при первой заливке
  originals/
  backup/
  data/                  # SQLite/Postgres volumes, при необходимости
```

## Структура репозитория

```
yauza-photofactory/
  PROJECT.md           # этот файл
  config.example.yaml
  docs/
    ROADMAP.md         # этапы и статусы
    VPS-CLEANUP.md     # подготовка VPS
    modules/           # по одному файлу на модуль
  src/
    photofactory/      # пакет Python (появится по этапам)
```

## Этапы (текущий план)

См. [docs/ROADMAP.md](docs/ROADMAP.md). Статусы обновляем по мере работы.

| # | Модуль | Статус |
|---|--------|--------|
| 0 | Документация + конфиг | done |
| 1 | VPS: очистка, базовая ОС, vsftpd | done |
| 2 | Batch watcher (тишина 60 с, перенос, backup) | done |
| 3 | PostgreSQL + модели batch/photo | done |
| 4 | FastAPI: список пачек, ZIP, очистка incoming | done |
| 5 | Уведомления (In-App / MAX / PWA) | в работе |
| 6 | Яндекс.Диск | done |
| 7 | UI/UX кабинета бильда | в работе |
| 8 | Архив originals + TTL | в работе |

## FTP: одна учётка, много фотографов

- **Один** системный/FTP-пользователь (например `upload`) с паролем, известным всем фотографам на мероприятие.
- **Chroot** в `/srv/yauza/incoming` (или аналог).
- **Upload-only**: без удаления/переименования с камеры.
- Подпапка `<photographer>` = имя, **настроенное в фотоаппарате**; сервер **не создаёт** список фотографов заранее — только принимает файлы.
- Watcher при первом файле в новой подпапке может создать запись `photographer` в БД (этап 3).

## Batch watcher (логика)

1. Следит за `incoming/*/*.jpg` (и регистр .JPG).
2. При новом файле — сброс таймера тишины для этой подпапки.
3. Если **N секунд** (config: `batch.silence_seconds`, default **60**) нет новых файлов — **фиксация пачки**:
   - дедупликация (размер + имя или хеш — уточнить в модуле 2);
   - копия в `originals/...` (без удаления из `incoming`);
   - локальная копия в `backup/` только если явно включена;
   - запись в БД;
   - постановка задач: Telegram, Яндекс.Диск.
4. Очистка `incoming/<photographer>/`:
   - вручную через FileZilla (олдскул-сценарий),
   - или автоматически при скачивании в кабинете (`Скачать все` / `Скачать выбранное`).

## Продуктовые сценарии (текущий этап)

### Сценарий 1 — oldschool / fallback

- Фотограф грузит в `incoming/<photographer>/`.
- После фиксации пачки бильд получает push (PWA/WPS) и забирает файлы напрямую через FileZilla.
- Всё входящее зеркалируется на Яндекс.Диск.
- Локальный `backup/` на сервере отключён.

### Сценарий 2 — кабинет бильда без FTP

- Фотограф грузит в `incoming/<photographer>/`.
- Бильд получает push, открывает кабинет и видит только актуальные пакеты (которые реально лежат в `incoming`).
- Для пакета доступны:
  - `Скачать все` (все фото пакета),
  - `Скачать выбранное` (только отмеченные чекбоксами).
- Имена архивов:
  - все: `<photographer>-<DD>-<MM>-<HHMM>-<daily_seq>.zip`
  - выбранное: `<photographer>-<DD>-<MM>-<HHMM>-selected_<daily_counter>-<daily_seq>.zip`
- После скачивания:
  - `Скачать все` удаляет все файлы пакета из `incoming`,
  - `Скачать выбранное` удаляет только выбранные скачанные файлы.

## Этап 8 — Архив и TTL хранения

После стабилизации сценариев 1+2 вводим управляемый архив `originals`:

1. Удаление из `incoming` (через кабинет или вручную в FileZilla) **не удаляет сразу** файлы из `originals`.
2. Для пакета фиксируется момент "пакет ушёл из incoming" и с этого момента начинает считаться TTL.
3. По истечении TTL пакет автоочищается из `originals`.
4. В кабинете появляется вкладка `Архив`:
   - видны пакеты, уже ушедшие из `incoming`, но ещё не удалённые по TTL;
   - возможна повторная загрузка пакета в период TTL.
5. В `Настройках`:
   - выбор TTL из значений:
     - `1ч, 2ч, 3ч, 4ч, 5ч, 6ч, 8ч, 12ч, 24ч, 36ч`;
   - значение по умолчанию: `3ч`;
   - поле лимита архива в ГБ;
   - кнопка `Очистить архив`.
6. В `Архиве` отображается индикатор заполнения:
   - `занято / лимит` в ГБ;
   - процент заполнения.

## FastAPI

Достаточно для REST, авторизации бильдов, отдачи списков и ZIP. Тяжёлую работу (watch, очереди) — **отдельные процессы**, не внутри воркера uvicorn.

## Конфигурация

Файл `config.yaml` (не в git). Все переменные дублируются в `config.example.yaml` с комментариями.

## Безопасность (минимум)

- Секреты только в `config.yaml` / env на сервере.
- FTP без TLS — ограничение камер; компенсируем firewall, сильный пароль, по возможности allowlist IP на мероприятии.
- Веб и API — HTTPS, JWT/сессии для бильдов (этап 4).

## Связь с Cursor

- Правила и память: `.cursor/rules/` при необходимости.
- Каждый завершённый этап — обновление `docs/ROADMAP.md` и `docs/modules/*.md`.
- Не раздувать один PR: один модуль — один логический коммит (по запросу).

## Долгосрочный ориентир

Полное видение проекта (роли, сценарии, архитектура, схема БД, UI) — в [`docs/reference/legacy-spec.md`](docs/reference/legacy-spec.md). Это **не ТЗ MVP**; берём оттуда детали по мере выхода каждой фазы и переносим в `docs/modules/*`.

## История решений

| Дата | Решение |
|------|---------|
| 2026-05-21 | Старт репозитория, Python + FastAPI, тишина 60 с, Telegram первым, FTP — один пароль / подпапка = фотограф |
| 2026-05-21 | Сохранены прошлые наработки в `docs/reference/legacy-spec.md` как долгосрочный ориентир |
| 2026-05-21 | **Расхождение с legacy:** `ftp_folder_name` теперь **задаётся в камере фотографом** и подпапка создаётся при первой заливке (а не пре-генерируется супербильдом как `ph_a47f3b9c`). Уникальность контролируется операционно. Запись `photographers` в БД создаётся watcher’ом при первом файле от нового имени. |
